---
id: simpo-cli-skill-claude-code-install
title: Install SimpoCLI Skill for Claude Code
sidebar_label: Installation
---

# Install SimpoCLI Skill for Claude Code

The `simpo-cli` Claude Code plugin packages the same `simpo` Skill and
references used by the Codex plugin. It routes SIMPO work through the installed
SimpoCLI and does not contain an API Token.

## Prerequisites

Verify Claude Code and SimpoCLI:

```text
claude --help
simpo --version
simpo --help
```

Configure a Token interactively when needed:

```text
simpo configure
simpo me
```

Never paste the Token into a Claude prompt.

## Install from the SIMPO Marketplace

Start Claude Code and add the repository Marketplace:

```text
/plugin marketplace add Jakkwj/simpo-home@master
```

Install the plugin:

```text
/plugin install simpo-cli@simpo
```

Reload plugins if Claude Code requests it:

```text
/reload-plugins
```

The namespaced Skill command is `/simpo-cli:simpo`.

## Verify

Run this in a new or reloaded session:

```text
/simpo-cli:simpo What does this Skill do? Do not change anything.
```

The response should explain current-version help discovery, Token-scope checks,
safe SIMPO resource operations, and JSON result verification. Continue with
[Claude Code usage and examples](./simpo-cli-skill-claude-code-usage).

## Update

Refresh the Marketplace and update the plugin:

```text
/plugin marketplace update simpo
```

Then open `/plugin`, select **Installed**, choose `simpo-cli@simpo`, and select
**Update**. Reload plugins if prompted.

## Manual fallback

If the installed Claude Code does not support plugins, update it first. As a
temporary local fallback, copy the complete
`plugins/simpo-cli/skills/simpo` directory to
`~/.claude/skills/simpo`; do not copy only `SKILL.md`, because the workflow
needs its `references/` files. The fallback command is `/simpo` without the
plugin namespace.
