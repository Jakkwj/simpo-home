---
id: simpo-cli-skill-codex-install
title: Install SimpoCLI Skill for Codex
sidebar_label: Installation
---

# Install SimpoCLI Skill for Codex

Install the Skill directly into Codex's Skill directory.

## Prerequisites

Verify Codex and the separately installed SimpoCLI:

```text
codex --help
simpo --version
simpo --help
```

Configure a Token interactively if needed:

```text
simpo configure
simpo me
```

Never paste the Token into Codex.

## Install from this repository

The recommended route is to ask Codex's built-in Skill Installer to install the
Skill from GitHub:

```text
Use $skill-installer to install this Skill from:
https://github.com/Jakkwj/simpo-home/tree/master/plugins/simpo/skills/simpo
```

If the built-in installer is unavailable, copy the complete Skill directory to
Codex's local Skill directory. Keep `references/` with `SKILL.md`:

```bash
mkdir -p "${CODEX_HOME:-$HOME/.codex}/skills"
cp -R plugins/simpo/skills/simpo \
  "${CODEX_HOME:-$HOME/.codex}/skills/simpo"
```

Start a new Codex session after installation.

## Verify

Ask:

```text
What does $simpo do? Do not change anything.
```

Installation succeeded if Codex explains that it discovers the installed CLI
interface and safely operates SIMPO resources.

## Update

The Skill Installer does not overwrite an existing Skill. Back up or remove the
installed `$CODEX_HOME/skills/simpo` directory, install the latest GitHub copy,
and start a new Codex session.
