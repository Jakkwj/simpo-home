---
id: simpo-cli-skill-codex-install
title: 在 Codex 中安装 SimpoCLI Skill
sidebar_label: 安装
---

# 在 Codex 中安装 SimpoCLI Skill

将 Skill 直接安装到 Codex 的 Skill 目录。

## 安装前准备

确认 Codex 与单独安装的 SimpoCLI 可用：

```text
codex --help
simpo --version
simpo --help
```

如果还没有配置 Token，请在终端中交互执行：

```text
simpo configure
simpo me
```

不要把 Token 粘贴到 Codex 中。

## 从本仓库安装

推荐让 Codex 内置的 Skill Installer 从 GitHub 安装：

```text
Use $skill-installer to install this Skill from:
https://github.com/Jakkwj/simpo-home/tree/master/plugins/simpo/skills/simpo
```

如果内置安装器不可用，可将完整 Skill 目录复制到 Codex 的本地 Skill 目录；必须保留
`references/` 与 `SKILL.md`：

```bash
mkdir -p "${CODEX_HOME:-$HOME/.codex}/skills"
cp -R plugins/simpo/skills/simpo \
  "${CODEX_HOME:-$HOME/.codex}/skills/simpo"
```

安装后重新启动 Codex 会话。

## 验证

询问：

```text
请说明 $simpo 的作用，不要执行任何修改。
```

如果 Codex 能说明它会识别当前安装的 CLI 接口并安全操作 SIMPO 资源，则安装成功。

## 更新

Skill Installer 不会覆盖已经存在的 Skill。请先备份或删除已安装的
`$CODEX_HOME/skills/simpo` 目录，再安装最新 GitHub 副本并重新启动 Codex 会话。
