---
# id: Tips

---


import config from '@generated/docusaurus.config'

# 提示


- 要在 BioModel.Matrix/DataSet.Connetions.Flow 中进行**幂**计算，请使用 `**`（而不是 `^`）。
- 将网页浏览器的**退格**键设置为**不**返回上一页，这样您在 BioModel.Matrix/DataSet.Connetions.Flow 中创建表格时就会被打断。
